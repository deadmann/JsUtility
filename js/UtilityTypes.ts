/**
 * Created by Hassan on 1/11/2017.
 */
type IterationOptionString = 'first' | 'last' | 'all';
enum IterationOption{
    First,
    Last,
    All
}

enum DateDifferencesType{
    Milliseconds,
    Seconds,
    Minutes,
    Hours,
    Days,
    Weeks,
    Months,
    Years,
}